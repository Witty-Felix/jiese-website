# Cloudflare Pages 响应头规则
#
# 目的：前端脚本与样式必须强制回源校验，避免浏览器/CDN 长期挂着旧副本，
# 出现「修复已上线但用户仍跑旧前端」的假象（ADR-0007）。
# no-cache = 每次使用前带 ETag 回源校验：内容没变走 304（开销极小），变了立刻拿到新版本。
#
# 有了这条规则，index.html 里的 ?v= 版本号不再是更新前端的必要手段（它只用于一次性
# 冲掉规则生效前已在浏览器里缓存的旧副本），因此不存在「两处版本号要手工同步」的漂移问题。

/app.js
  Cache-Control: no-cache

/styles.css
  Cache-Control: no-cache
